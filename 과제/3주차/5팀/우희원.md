<img width="2088" height="1430" alt="스크린샷 2026-05-08 112919" src="https://github.com/user-attachments/assets/86b2e7fd-1b31-48e8-9c2f-866f6884f442" />
<img width="2208" height="1414" alt="스크린샷 2026-05-08 112911" src="https://github.com/user-attachments/assets/9b7acc63-ab19-4c76-8aae-62ced51ff1a9" />

설정한 임계값 : 거리 100cm

거리 100cm 초과할 때 : 초록불 동작
거리 100cm 이하일 때 : 빨간불 동작


<초음파센서 / 코드>

int sensor = 8;
int green = 9;
int red = 10;
int limit = 100;//임계 거리기준

void setup() {
  pinMode(green, OUTPUT);
  pinMode(red, OUTPUT);
  Serial.begin(9600);
}

void loop() {
  long t;
  int distance;

  pinMode(sensor, OUTPUT);
  digitalWrite(sensor, LOW);
  delayMicroseconds(2);
  digitalWrite(sensor, HIGH);
  delayMicroseconds(5);
  digitalWrite(sensor, LOW);

  pinMode(sensor, INPUT);
  t = pulseIn(sensor, HIGH);
  distance = t * 0.034 / 2;
  Serial.print(distance);

  if (distance > limit) {
    digitalWrite(green, HIGH);
    digitalWrite(red, LOW);
  }
  else {
    digitalWrite(green, LOW);
    digitalWrite(red, HIGH);
  }



  delay(200);
}
